apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-daily
  namespace: app
spec:
  # CNPG cron has a leading SECONDS field — this is 03:00 daily, not 00:03.
  schedule: "0 0 3 * * *"
  backupOwnerReference: self
  immediate: true
  cluster:
    name: postgres
